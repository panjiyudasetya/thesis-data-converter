
class StubCriteria:

    def load(self) -> None:
        """
        Stub interface for creating criteria data of the clients
        who has social anxiety disorder,
        and then stores them either to remote database or local storage.
        """
        print('Stub `loaders.Criteria.load` is called.')
